{
  "name": "monorepo-frontend",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "prepare": "git config --local core.hooksPath .githooks"
  },
  "devDependencies": {
    "eslint": "^8.9.0"
  }
}
